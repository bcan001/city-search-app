<div style="color:white;text-align:center;">
	<h1>My Profile</h1>

	<h3><%= current_user.name %></h3>

	<% @cities.each do |c| %>
		<p><%= link_to c.name, show_path(c), style: "color:white;" %></p>
	<% end %>

</div>
